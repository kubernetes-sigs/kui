#!/usr/bin/env node

// get node off the argv
process.argv.shift()

process.argv.push(`--command-context=["wsk"]`)
process.env.KUI_HEADLESS = true

// process.env.DEFAULT_TO_UI = true

require('../build/packages/app/src/main/main').main(process.argv)
