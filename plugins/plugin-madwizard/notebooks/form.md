# Free-form Choice via Forms

You may also offer a form to users, so that they can provide free-form text.

=== "How many eggs [default: 3]"
    ```shell
    echo "got ${choice} eggs"
    ```
    
=== "How much toast [default: all]"
    ```shell
    echo "got ${choice} toast"
    ```
