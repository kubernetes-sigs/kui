# Copy of pod.yaml without file extension for test
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: nginx
spec:
  containers:
  - name: name
    image: quay.io/bitnami/nginx
    ports:
    - containerPort: 80
