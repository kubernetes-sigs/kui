apiVersion: v1
kind: Pod
metadata:
  name: kui-crashy
spec:
  containers:
  - name: crashy
    image: quay.io/bitnami/nginx:1.18.0
    command: ["/bin/sh", "-c", "echo logtest; sleep 5; echo exiting; exit"]
