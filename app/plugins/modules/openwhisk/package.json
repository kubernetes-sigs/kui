{
  "name": "openwhisk",
  "version": "0.1.0",
  "description": "IBM Cloud shell plugin for OpenWhisk",
  "main": "plugin/plugin.js",
  "scripts": {
    "postinstall": "npx tsc",
    "pretest": "npx tsc",
    "compilenowatch": "npx tsc",
    "compile": "npx tsc -watch -p ./",
    "lint": "npx tslint -c tslint.json 'src/**/*.ts'"
  },
  "keywords": [
    "openwhisk",
    "kubernetes"
  ],
  "author": "Nick Mitchell",
  "license": "Apache-2.0",
  "dependencies": {
    "debug": "^4.1.1",
    "openwhisk": "git://github.com/starpit/openwhisk-client-js.git#add_client_timeout4"
  },
  "devDependencies": {
    "@types/swagger-schema-official": "^2.0.14",
    "tslint": "^5.12.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.2.2"
  }
}
