# Copy of pod.yaml without file extension for test
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: nginx
  annotations:
    electron-shell.org/description: Create an nginx pod
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
